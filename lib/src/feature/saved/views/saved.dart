import 'package:flutter/material.dart';

class SavedFragment extends StatelessWidget {
  const SavedFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Saved", style: TextStyle(color: Colors.white),);
  }
}